[project]
name = "revit-ai-area-assistant"
version = "0.1.0"
description = "Python AI Agent for the Revit 2026 C# Area Assistant"
requires-python = ">=3.10"

[project.optional-dependencies]
test = ["jsonschema>=4.21,<5"]

[tool.setuptools.packages.find]
include = ["area_assistant_agent*"]
